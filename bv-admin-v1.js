(()=>{const $=i=>document.getElementById(i),E=v=>String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));window.renderProductsAdmin=()=>{let b=$('manage');if(!b)return;b.innerHTML=(window.products||[]).filter(x=>x.active!==false).map(x=>`<div class="line"><span><b>${E(x.name)}</b> — R$ ${(+x.price||0).toFixed(2).replace('.',',')}</span><button type="button" onclick="removeProduct('${E(x.id)}')">Excluir</button></div>`).join('')||'<p class="muted">Nenhum produto cadastrado.</p>'};window.BV_USERS_RENDER=async()=>{let b=$('userPermissions');if(!b||!window.admin?.())return;let r=await window.BV_ADMIN_USERS?.();if(r?.error)return b.innerHTML='<p class="muted">'+E(r.error)+'</p>';let a=r?.users||[],q=($('userSearch')?.value||'').toLowerCase();if($('userCount'))$('userCount').textContent=a.length+' usuários';b.innerHTML=a.filter(x=>(x.name||'').toLowerCase().includes(q)||(x.email||'').toLowerCase().includes(q)).map(x=>`<div class="line"><span><b>${E(x.name||x.email)}</b><small>${E(x.email||'')}</small></span><select onchange="changeUserRole('${E(x.id)}',this.value)"><option value="usuario" ${x.role==='usuario'?'selected':''}>Usuário</option><option value="motoboy" ${x.role==='motoboy'?'selected':''}>Motoboy</option><option value="administrador" ${x.role==='administrador'?'selected':''}>Administrador</option></select><button onclick="deleteUser('${E(x.id)}')">Excluir</button></div>`).join('')||'<p class="muted">Nenhum usuário.</p>'};window.renderUsers=()=>window.BV_USERS_RENDER?.();document.addEventListener('DOMContentLoaded',()=>{window.renderProductsAdmin?.();window.BV_USERS_RENDER?.()})})();